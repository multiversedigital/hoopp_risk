# HOOPP Risk Navigator - UI Style Guide

## 设计理念

借鉴 BlackRock Aladdin 的 "Invisible UI" 和 MSCI RiskMetrics 的专业仪表盘风格：

1. **数据优先** — UI 退居幕后，数据成为主角
2. **克制用色** — 只在关键状态使用强调色
3. **清晰层次** — 信息从概览到细节渐进展开
4. **专业质感** — 机构报告级别的排版

---

## 颜色系统

### 主色板 (Dark Theme)

```
背景层级:
  Level 0 (页面)     #0a0e14    纯黑偏蓝    ← 比之前更深
  Level 1 (卡片)     #12171f    深灰蓝      ← 微妙层次
  Level 2 (悬停)     #1a2332    中灰蓝      ← 交互反馈
  Level 3 (边框)     #262f3d    浅灰蓝      ← 分隔线

文字层级:
  Primary            #f0f4f8    近白        ← 主要数字、标题
  Secondary          #94a3b8    灰蓝        ← 标签、说明
  Tertiary           #64748b    暗灰        ← 次要信息

语义色 (状态):
  Positive           #10b981    翠绿        ← 正向变化、OK
  Negative           #ef4444    亮红        ← 负向变化、BREACH
  Warning            #f59e0b    琥珀        ← 警告、WARN
  Info               #3b82f6    蓝          ← 中性信息

强调色:
  Accent Primary     #6366f1    靛蓝        ← 主要交互、选中态
  Accent Secondary   #8b5cf6    紫          ← 次要强调
```

### 数据可视化色板

```
单色渐变 (用于饼图、柱状图):
  ['#6366f1', '#818cf8', '#a5b4fc', '#c7d2fe', '#e0e7ff']

双色对比 (用于 Actual vs Target):
  Actual:  #6366f1  靛蓝
  Target:  #f59e0b  琥珀

多类别 (用于资产类别):
  Fixed Income:      #3b82f6  蓝
  Public Equities:   #10b981  绿
  Real Estate:       #f59e0b  琥珀
  Infrastructure:    #8b5cf6  紫
  Private Credit:    #ec4899  粉
  Cash:              #64748b  灰
```

---

## 排版系统

### 字体

```css
font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

### 字号层级

```
Display     2.5rem (40px)   700   ← 页面大标题 (很少用)
H1          1.75rem (28px)  600   ← Tab 标题
H2          1.25rem (20px)  600   ← Section 标题
H3          1rem (16px)     600   ← 卡片标题
Body        0.875rem (14px) 400   ← 正文
Caption     0.75rem (12px)  400   ← 标签、说明
Micro       0.625rem (10px) 500   ← 图表刻度

数字专用:
  KPI 大数字   2rem (32px)    700   font-variant-numeric: tabular-nums
  表格数字     0.875rem       400   font-variant-numeric: tabular-nums
```

### 行高

```
紧凑 (数字、KPI):  1.2
正常 (正文):       1.5
宽松 (段落):       1.75
```

---

## 间距系统

### 基础单位: 4px

```
xs    4px     ← 紧凑元素内边距
sm    8px     ← 小间隙
md    16px    ← 标准间距
lg    24px    ← 区块间隔
xl    32px    ← 大区块间隔
2xl   48px    ← 页面级间隔
```

### 卡片规范

```
padding:        20px (lg + xs)
border-radius:  8px
border:         1px solid #262f3d
gap (内部):     16px
```

---

## 组件规范

### KPI 卡片

```
┌─────────────────────────┐
│  FUNDED STATUS          │  ← Caption, Secondary, uppercase
│  111.2%                 │  ← Display number, Primary, 32px
│  ▲ +0.3%                │  ← Delta, Positive/Negative color
└─────────────────────────┘

高度: auto (内容撑开)
最小宽度: 140px
背景: Level 1
边框: 1px solid Level 3
```

### 数据表格

```
┌──────────────────────────────────────────────────────┐
│  COLUMN A     COLUMN B     COLUMN C     STATUS       │  ← Header: Level 2, Caption, uppercase
├──────────────────────────────────────────────────────┤
│  Value 1      Value 2      Value 3      🟢 OK        │  ← Row: Level 1, Body
│  Value 1      Value 2      Value 3      🔴 BREACH    │
│  Value 1      Value 2      Value 3      🟡 WARN      │
└──────────────────────────────────────────────────────┘

行高: 44px
Header 背景: Level 2
行分隔: 1px solid Level 3
悬停: Level 2 背景
```

### 状态徽章

```
🟢 OK      背景: rgba(16, 185, 129, 0.15)  文字: #10b981
🟡 WARN    背景: rgba(245, 158, 11, 0.15)  文字: #f59e0b
🔴 BREACH  背景: rgba(239, 68, 68, 0.15)   文字: #ef4444

padding: 4px 8px
border-radius: 4px
font-size: 12px
font-weight: 500
```

### 图表通用设置

```
背景: transparent (继承页面)
网格线: #262f3d, 0.5px
坐标轴文字: #64748b, 10px
图例: #94a3b8, 12px
Tooltip 背景: #1a2332
Tooltip 边框: #262f3d
```

---

## 布局规范

### 页面结构

```
┌─────────────────────────────────────────────────────────┐
│  Sidebar (260px fixed)  │  Main Content (fluid)         │
│                         │                               │
│  Logo                   │  Tab Bar                      │
│  ─────                  │  ───────────────────────      │
│  Navigation             │                               │
│  ─────                  │  KPI Row (full width)         │
│  Date Picker            │  ───────────────────────      │
│  ─────                  │                               │
│  Info                   │  Content Grid                 │
│                         │  (responsive columns)         │
│                         │                               │
└─────────────────────────────────────────────────────────┘
```

### 响应式断点

```
Desktop:   > 1200px   3-4 列 KPI, 2 列图表
Tablet:    768-1200px 2-3 列 KPI, 1-2 列图表  
Mobile:    < 768px    1 列堆叠
```

### Tab 内布局模板

```
Row 1: KPI Cards
  - 4-5 个卡片一行
  - 使用 st.columns() 等分

Row 2: Primary Visualization
  - 满宽或 2 列
  - 主要图表放这里

Row 3: Secondary Content
  - 2-3 列布局
  - 表格、次要图表
```

---

## 交互规范

### 悬停状态

```
卡片:   背景变为 Level 2, transition 0.15s
按钮:   背景变亮 10%, cursor pointer
表格行: 背景变为 Level 2
```

### 选中状态

```
Tab:    底部 2px 实线, Accent Primary
按钮:   背景 Accent Primary, 文字白色
```

### 加载状态

```
使用 Streamlit spinner
或自定义 skeleton 占位
```

---

## 最佳实践

### DO ✅

- 数字右对齐，文字左对齐
- 使用 tabular-nums 让数字等宽
- 大数字用千分符 ($123,456)
- 百分比保留 1-2 位小数
- 状态用颜色 + 图标双重编码

### DON'T ❌

- 不要用纯黑 #000000
- 不要用纯白 #ffffff
- 不要超过 3 种强调色
- 不要在深色背景上用细字体
- 不要让图表比数据更抢眼
